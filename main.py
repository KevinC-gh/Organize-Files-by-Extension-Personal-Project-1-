import os

def main():
    
    print("============ SORT BY EXTENSION ============")
    source_folder = input("Source folder (enter full path of folder where you're moving files FROM): ")
    dest_folder = input("Destination folder (enter full path of folder where you want to move files TO: ")
    